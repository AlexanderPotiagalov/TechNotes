const allowedOrigins = [
  // Define an array of allowed origins for CORS
  "https://technoteshub-dis4.onrender.com", // Production URL
  "http://localhost:3001", // Development URL
];

module.exports = allowedOrigins; // export the allowed origins array for use in other modules
